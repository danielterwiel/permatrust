name: CI
on:
  push:
    branches: [main]
    paths-ignore:
      - "**.md"
      - "docs/**"
  pull_request:
    paths-ignore:
      - "**.md"
      - "docs/**"

jobs:
  setup:
    uses: ./.github/workflows/setup.yml

  quality:
    needs: setup
    uses: ./.github/workflows/quality.yml

  # test:
  #   needs: [setup, build]
  #   uses: ./.github/workflows/test.yml
