{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "pt",
  "scripts": {
    "build:shared": "./build-utils/build-shared.sh",
    "build": "npm run build --workspaces --if-present",
    "build:icons": "npm run build:icons --workspaces --if-present",
    "icons:add": "npm run icons:add --workspaces --if-present",
    "prebuild": "npm run prebuild --workspaces --if-present",
    "pretest": "npm run prebuild --workspaces --if-present",
    "start": "npm start --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "format": "npm format --workspaces --if-present"
  },
  "type": "module",
  "workspaces": [
    "src/pt_frontend"
  ]
}
